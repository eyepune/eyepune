# ⚠️ DEPRECATED — This workflow has been consolidated into ai-automation.yml
# The AI Blog generation is now triggered by: .github/workflows/ai-automation.yml (runs at 09:00 UTC / 2:30 PM IST)
# Keeping this file to avoid a 404 in GitHub UI — but the schedule is disabled.
#
# DO NOT re-enable this cron — doing so will trigger DOUBLE blog posts per day.

name: "[DISABLED] Daily AI Blog Generator - Consolidated"

on:
  workflow_dispatch: # Manual trigger only for emergency use

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is disabled
        run: |
          echo "⚠️ This workflow is DISABLED. The blog generation now runs via ai-automation.yml"
          echo "See: .github/workflows/ai-automation.yml"
